#include "green_thread.h"
#include "internal.h"
#include <unistd.h>

// Placeholder. Real one needs a sorted list of wake times.
void check_timers(void) {
  // Called by scheduler idle loop or signal handler
}
